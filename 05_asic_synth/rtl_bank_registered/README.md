# Per-bank write-register RTL (Approach A)

Copy of [`01_rtl/`](../../01_rtl/) used by the Fusion Compiler file list. Only `BUFFER_WRAPPER_SR.sv` differs from `rtl_bank_internal_slice/` (no intra-bank 4×64 split).
