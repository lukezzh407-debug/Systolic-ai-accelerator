# 06 — Gate-level simulation

Placeholder. Method: swap the accelerator RTL for the Fusion Compiler netlist **on a licensed machine**, plus foundry stdcell simulation models. Netlists and cell models stay out of git (see root `.gitignore` and `05_asic_synth/pdk/`).
